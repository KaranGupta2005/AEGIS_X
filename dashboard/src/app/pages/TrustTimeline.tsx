import React from 'react'

const TrustTimeline: React.FC = () => (
  <div>
    <h1 style={{ fontSize: 24, fontWeight: 700, fontFamily: 'Space Grotesk', marginBottom: 8 }}>Trust Timeline</h1>
    <p style={{ color: 'var(--text-muted)', fontFamily: 'JetBrains Mono', fontSize: 12 }}>Trust score evolution over session lifetime</p>
  </div>
)

export default TrustTimeline
