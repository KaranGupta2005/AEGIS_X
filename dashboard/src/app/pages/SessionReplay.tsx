import React from 'react'

const SessionReplay: React.FC = () => (
  <div>
    <h1 style={{ fontSize: 24, fontWeight: 700, fontFamily: 'Space Grotesk', marginBottom: 8 }}>Session Replay</h1>
    <p style={{ color: 'var(--text-muted)', fontFamily: 'JetBrains Mono', fontSize: 12 }}>Attack progression timeline</p>
  </div>
)

export default SessionReplay
